{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "PY=python3; [ -x .venv/bin/python3 ] && PY=.venv/bin/python3; PORT=\"${API_PORT:-${PORT:-8011}}\"; exec \"$PY\" -m uvicorn app.main:app --reload --host 0.0.0.0 --port \"$PORT\"",
    "build": "PY=python3; [ -x .venv/bin/python3 ] && PY=.venv/bin/python3; \"$PY\" -m compileall app && mkdir -p dist && echo > dist/.gitkeep",
    "lint": "PY=python3; [ -x .venv/bin/python3 ] && PY=.venv/bin/python3; \"$PY\" -m compileall app"
  }
}
