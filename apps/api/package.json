{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "python3 -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "python3 -m compileall app && mkdir -p dist && touch dist/.gitkeep",
    "lint": "python3 -m compileall app"
  }
}
