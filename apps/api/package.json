{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "postinstall": "node scripts/ensure-venv.cjs",
    "dev": "node scripts/with-venv.cjs -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8011",
    "build": "node scripts/with-venv.cjs -m compileall app && mkdir -p dist && touch dist/.gitkeep",
    "lint": "node scripts/with-venv.cjs -m compileall app"
  }
}
