{
  "name": "desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "desktop:dev": "tauri dev",
    "desktop:build": "node scripts/build-tauri.mjs",
    "desktop:build:debug": "node scripts/build-tauri.mjs --debug"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
