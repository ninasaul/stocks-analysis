{
  "name": "stocks-analysis-simple",
  "version": "0.0.0",
  "private": true,
  "packageManager": "npm@11.6.1",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  },
  "workspaces": [
    "apps/*"
  ],
  "engines": {
    "node": ">=20"
  }
}