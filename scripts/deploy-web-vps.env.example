# 复制为 deploy-web-vps.env 后按需填写（deploy-web-vps.env 已被 git 忽略）。
#
# --- 腾讯云 CVM / 轻量应用服务器（常见情况）---
# 登录名多为 ubuntu，不是 root：
# VPS_HOST=ubuntu@203.0.113.10
# SSH_IDENTITY_FILE=/home/你的用户/.ssh/server.pem
# 在实例上配置 Docker 镜像加速（VPC 内访问，加快 docker build 拉镜像）：
#   https://cloud.tencent.com/document/product/1207/45596
# /etc/docker/daemon.json 示例：
#   "registry-mirrors": ["https://mirror.ccs.tencentyun.com"]
# 镜像加速生效后，一般可不再设置下面的 DOCKER_NODE_IMAGE，直接用 Dockerfile 里的默认 node 镜像名构建。
#
# --- 任意 VPS ---
# 可选；默认在登录用户家目录下 TuringFin（即 ~/TuringFin）：
# REMOTE_REPO_SUBDIR=TuringFin
# 或指定 VPS 上的绝对路径：
# REMOTE_REPO_ABSPATH=/var/www/TuringFin
#
# 若 VPS 仍无法访问 Docker Hub，可传入完整的基础镜像名作为 NODE_IMAGE：
# DOCKER_NODE_IMAGE=docker.m.daocloud.io/library/node:20-bookworm-slim
#
# WEB_IMAGE=stocks-web:latest
# WEB_PORT=3000
# NEXT_PUBLIC_API_BASE_URL=https://api.example.com
# NEXT_PUBLIC_SITE_URL=https://www.example.com
# NEXT_PUBLIC_USE_MOCK_FLOW=false
# RSYNC_DELETE=0
# SKIP_RSYNC=0
#
# 仅测试 SSH：
#   ./scripts/test-vps-ssh.sh
